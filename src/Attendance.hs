module Attendance
  (createAttendant,
  deleteAttendant,
  listAttendants,
  attend,
  hide,
  listAttendancies)
where

createAttendant :: a -> a
createAttendant a = a

deleteAttendant :: a -> a
deleteAttendant a = a

listAttendants :: a -> a
listAttendants a = a

attend :: a -> a
attend a = a

hide :: a -> a
hide a = a

listAttendancies :: a -> a
listAttendancies a = a
